- if @post.errors.any?
  %h2= pluralize(@post.errors.count, "error")
  %ul
  - @post.errors.full_messages.each do |msg|
    %li= msg

%br

%h1 Editing Product
= render partial: 'form'

= link_to 'Back', posts_path

